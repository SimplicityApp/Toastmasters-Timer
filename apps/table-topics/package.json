{
  "name": "toastmusters-table-topics",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Table Topics question generator — static site rendered from content/questions.json, served by its own Cloudflare Worker.",
  "scripts": {
    "validate": "node scripts/validate-questions.mjs",
    "build": "node scripts/build.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@toastmaster-timer/shared": "file:../../packages/shared"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
